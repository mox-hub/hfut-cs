/* example1-7 */
#include<stdio.h>

int main(void)
{
    int a = -1;
    unsigned b = 1;
    char c = 'A';

    printf("a = %u, %d, %o, %x\n", a, a, a, a);
    printf("b = %u, %d, %o, %x\n", b, b, b, b);
    printf("c = %u, %d, %o, %x\n", c, c, c, c);

    return 0;
}
